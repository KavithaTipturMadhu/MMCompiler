int test(){
char e,f;

e='w';
f='a';

if((g << 1) > (g >>1)){
    if((int)e > x){
        return e;
    }else{
        if(x*y > x)
            return x;
        else
            return y;
    }
}

return a;
}
